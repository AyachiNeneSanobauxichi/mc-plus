# 进入 packages 目录
cd packages

# 创建所需的子目录
mkdir -p components core docs hooks theme utils

# 初始化 pnpm 
for dir in components core docs hooks theme utils; do
	cd $dir
	pnpm init
	cd ..
done

echo "init succeed!"
